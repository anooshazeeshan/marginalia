/**
 * Seed script placeholder. Once the Passage model and CRUD exist,
 * this will populate the dev database with a handful of realistic
 * sample Passages so the feed/demo never looks empty.
 */
async function main() {
  console.log("Seed script not yet implemented.");
}

main();
