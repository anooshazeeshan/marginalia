/**
 * Placeholder page — foundation phase only.
 * Real UI/feature implementation comes in a later milestone.
 */
export default function SettingsPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-paper text-ink">
      <p className="font-sans text-sm opacity-60">SettingsPage — not yet implemented</p>
    </main>
  );
}
